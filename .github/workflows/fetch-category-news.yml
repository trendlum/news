name: Fetch Category News

on:
  workflow_dispatch:
    inputs:
      category:
        description: 'Category slug to fetch'
        required: true
        type: string
      ingest_news_raw:
        description: 'Persist fetched news into news_raw'
        required: true
        default: true
        type: boolean
      persist_taxonomy:
        description: 'Persist taxonomy after fetch'
        required: true
        default: false
        type: boolean

concurrency:
  group: fetch-category-news-${{ github.ref }}-${{ inputs.category }}
  cancel-in-progress: true

jobs:
  fetch-category:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: npm run check

      - name: Public safety check
        run: npm run check:public

      - name: Fetch selected category
        env:
          INGEST_NEWS_RAW: ${{ inputs.ingest_news_raw }}
          PERSIST_TAXONOMY: ${{ inputs.persist_taxonomy }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        run: node --import tsx scripts/run.ts "${{ inputs.category }}"
